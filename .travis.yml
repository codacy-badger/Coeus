language: node_js

cache:
  directories:
    - ~/.npm
    - "node_modules"

node_js:
  - '13'

services:
  - mongodb
  - redis-server

git:
  depth: 3

branches:
  only:
    - master

script:
  - yarn
  - yarn seed
  - yarn test
